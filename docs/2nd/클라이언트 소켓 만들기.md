cpp 연습을 한동안 안했는데, 이전에 적었던 문서들이 무슨 소리를 하고 싶은지도 모르겠고
적당한 목표도 존재하지 않는 것 같아서 다시 새 문서를 작성한다. 

소켓 프로그래밍의 서버 측과 클라이언트 측 모두 구현하는 것도 의미가 있을테지만, 그보다 클라이언트 측 하나만 만들어서 객체화 시킨 뒤에 데이터를 주고 받는 것을 해보는 것이 더 의미있다고 판단했다. 
왜냐면 회사 제품에서 서버 측 역할을 하는 장비가 있기 때문에 ㅎㅎ;;

# 1) 클라이언트 측 객체화
우선, 클라이언트 측 프로그램을 객체화하기 위해서는 클라이언트 측 구현을 할 줄 알아야 할 것 같다. 
## 1.1> 클라이언트가 준비해야 될 게 무엇일까?
- 접속하고자 하는 서버의 IP, Port number 등 설정
- 소켓 생성
- `connect()`
- 서버에 메세지 송신 기능 구현
- 서버로부터 메세지 수신 기능 구현